<%@discounts.each do |discount|%>
  <section id="discount-<%= discount.id %>">
    Discount <%=discount.discount%>% on <%=discount.quantity%> items or more<br>
    <%=link_to "Edit Discount", edit_merchant_discount_path(discount.id)%><br><br>
  </section>
<%end%>

<%= link_to 'Add a new discount', new_merchant_discount_path%>

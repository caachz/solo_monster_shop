<%= form_for @discount, url: merchant_discount_path(@discount.id), method: :patch do |f|%>
  <%= f.label :discount %>
  <%= f.text_field :discount%><br>

  <%= f.label :quantity %>
  <%= f.text_field :quantity%><br>

  <%= f.submit %>
<%end%>
